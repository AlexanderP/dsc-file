--- azpainter-2.1.4.orig/translation/en
+++ azpainter-2.1.4/translation/en
@@ -203,8 +203,8 @@
 +=dim
 +=brighten
 +=difference
-+=luminous(add)
-+=luminous(dodge)
++=luminous (add)
++=luminous (dodge)
 
 #----------------
 # scale type
